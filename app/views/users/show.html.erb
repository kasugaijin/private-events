<% unless Invite.where(invitee_id: current_user.id).empty? %>
  <section class='section'>
    <div class="container">
      <div class="columns is-centered">
          <%= render 'invites' %>
      </div>
    </div>
  </section>
<% end %>

<section class='section'>
  <div class="container">
    <div class="columns">
        <%= render 'created' %>
        <%= render 'attending' %>
        <%= render 'attended' %>
    </div>
  </div>
</section>