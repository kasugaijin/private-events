<% if current_user.invites.exists? %>
  <section class='section'>
    <div class="container">
      <div class="columns is-centered">
          <%= render 'invites' %>
      </div>
    </div>
  </section>
<% end %>

<section class='section'>
  <div class="container">
    <div class="columns">
        <%= render 'created' %>
        <%= render 'attending' %>
        <%= render 'attended' %>
    </div>
  </div>
</section>