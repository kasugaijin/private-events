<div class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-4">
        <h2 class='title is-2'>Log in</h2>

        <%# users require further rails config to allow for :username log in. See devise docs.  %>

        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

          <div class="field">
            <div class="control">
               <%= f.input :email, 
                          required: false,
                          wrapper: false,
                          input_html: { class: 'input' }, 
                          label_html: { class:'label' } %>
            </div>
          </div>

          <div class="field">
            <div class="control">
              <%= f.input :password,
                          required: true,
                          wrapper: false,
                          input_html: { class: 'input' }, 
                          label_html: { class:'label' } %> <br />
              <%= f.input :remember_me, 
                          wrapper: false, 
                          as: :boolean if devise_mapping.rememberable? %>
            </div>
          </div>

          <div class="field">
            <div class="control">
              <%= f.button :submit, "Log in", class: 'button is-info is-medium' %>
            </div>
          </div>

        <% end %>
        <br />
        <%= render "devise/shared/links" %>

      </div>
    </div>
  </div>
</div>
